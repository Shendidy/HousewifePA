import React from 'react'
import StoreItems from '../components/storeItems/StoreItems'

export default StoreItemsScreen = ({ navigation, route }) => ( <StoreItems navigation={navigation} route={route} /> )